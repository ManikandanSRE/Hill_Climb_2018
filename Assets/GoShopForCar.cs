using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GoShopForCar : MonoBehaviour {
   
	public Transform trans;
	// Use this for initialization
	void Start () {
        //GameObject NewCar = Instantiate(gamemanager.CurrentCar, trans.position, trans.rotation, this.transform) as GameObject;
    }
	
	// Update is called once per frame
	void Update () {
		
	}
}
